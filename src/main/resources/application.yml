spring:
  sql:
    init:
      mode: never

jpa:
  show-sql: true
  properties:
    hibernate:
      format_sql: true
  defer-datasource-initialization: true

datasource:
  url: jdbc:h2:mem:testdb

h2:
  console:
    enabled: true

gemini:
  api-key: gen-lang-client-02625876883